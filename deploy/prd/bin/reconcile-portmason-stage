#!/usr/bin/env bash
set -euo pipefail

printf '%s\n' \
  'Portmason staging reconciliation is retired.' \
  'Run pm-setup from the repository root; production uses the pinned GitHub Actions workflow.' \
  >&2
