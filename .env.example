APP_SLUG=david
DEPLOY_ENV=dev
RUNTIME_ADAPTER_CODE=static-local
REPO_ROOT=/home/davidomer/code/david
CONTAINER_ROOT=/workspace
STACK=${APP_SLUG}${DEPLOY_ENV:+-${DEPLOY_ENV}}
LOCAL_DOMAIN=localtest.me

# ------------------------------
# anything below this line probably should not be edited manually
# ------------------------------
ENV_ROOT=${REPO_ROOT}
SITE_ROOT=${ENV_ROOT}/www
SITE_DIR=${SITE_ROOT}
LOG_DIR=${REPO_ROOT}/logs
APP_HOST=${STACK}.${LOCAL_DOMAIN}
URI_SCHEME=https:
SITE_URL=${URI_SCHEME}//${APP_HOST}

SITE_BRAND_NAME="David Park Omer"
SITE_CONTACT_EMAIL=davidomer@simplifai.team
GTM_CONTAINER_ID=
GA4_MEASUREMENT_ID=
ANALYTICS_NOTES="Analytics are intentionally blank in development."
PM_STATIC_EXCLUDE_DIRS=capture,deploy

CLIENT="David Park Omer"
OWNER=omer
OWNER_ALIAS=etalsolutions
MANAGED_BY=compose
LIFECYCLE=long-lived
DATA_CLASS=public
DATA_LIFECYCLE=persistent
