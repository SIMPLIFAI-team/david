diff --git a/content/case-studies.json b/content/case-studies.json
index d2a7449..a09c2e8 100644
--- a/content/case-studies.json
+++ b/content/case-studies.json
@@ -7,7 +7,7 @@
     "highlights": [
       "HTML5 cross-platform mobile data collection model",
       "Tenant-aware application and audit workflow concepts",
-      "Logical reporting layer over transactional data to reduce reporting overhead"
+      "Reporting overhead reduction from logical reporting layer"
     ],
     "url": "case-studies/multi-tenant-qa-auditing-mobile-data-collection.html"
   },
@@ -17,10 +17,22 @@
     "title": "Replayable vendor API ingestion and reporting layer",
     "summary": "A generalized data platform pattern showing efficient vendor API consumption, SharePoint Online caching, multi-tenant integration, and reporting design without sacrificing recovery or auditability.",
     "highlights": [
-      "Idempotent API consumer that reduces repetitive object pulls and supports replay",
-      "Performance-tuned SQL query shapes for reporting and integration workloads",
-      "Multi-tenant integration and reporting layer across vendor and operational data"
+      "Reduced repetitive object pulls with idempotent API consumer",
+      "Performance-tuned query shapes for reporting and ingestion workloads",
+      "Multi-tenant integration and reporting layer across vendor and ops data"
     ],
     "url": "case-studies/replayable-vendor-api-ingestion-reporting-layer.html"
-  }
+  },
+{
+"id": "corporate-website-and-portmason-platform-experience",
+"tag": "Web / Platform Case Study",
+"title": "Corporate website and Portmason platform experience",
+"summaryHtml": "<a href=\"https://www.etal.solutions\">www.etal.solutions</a> is a modern corporate website built as a fast, data-driven static experience that unifies services, brands, thought leadership, and the Portmason Platform within a maintainable publishing and deployment model.",
+"highlights": [
+"Self-registering catalog architecture for services, brands, promotions, and platform content",
+"Containerized local development with repeatable Portmason setup, rendering, and deployment workflows",
+"High-performance static delivery through GitHub Pages with modular content, responsive navigation, and environment-driven configuration"
+],
+"url": "case-studies/corporate-website-and-portmason-platform-experience.html"
+}
 ]
diff --git a/index.html b/index.html
index b00c319..6c0d541 100644
--- a/index.html
+++ b/index.html
@@ -50,7 +50,7 @@
     <div class="wrap nav-inner">
       <div class="brand">
         <a href="#top">David Park Omer</a>
-        <small>Software • Data Platforms</small>
+        <small>Software • Middleware • Data Platforms</small>
       </div>
       <div class="nav-links">
         <a href="#top">Home</a>
diff --git a/portmason/pm-clone-project b/portmason/pm-clone-project
index ee8570f..af98be1 100755
--- a/portmason/pm-clone-project
+++ b/portmason/pm-clone-project
@@ -1,6 +1,12 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
+: "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
+
 if [ "$#" -ne 1 ]; then
   echo "Usage: $0 NEW_FOLDER_NAME" >&2
   exit 1
diff --git a/portmason/pm-deploy-github b/portmason/pm-deploy-github
index 6f1a64c..9301d87 100755
--- a/portmason/pm-deploy-github
+++ b/portmason/pm-deploy-github
@@ -1,6 +1,12 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
+: "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
+
 # For GitHub Pages, deploy is normally handled by GitHub Actions. Locally this
 # prepares the static artifact directory and generated browser config.
 github_pages_prepare
diff --git a/portmason/pm-generate-config b/portmason/pm-generate-config
index 0c966ca..a491b94 100755
--- a/portmason/pm-generate-config
+++ b/portmason/pm-generate-config
@@ -1,7 +1,11 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
 : "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
 
 : "${ENV_FILE:?ENV_FILE is required}"
 : "${SITE_DIR:?SITE_DIR is required}"
diff --git a/portmason/pm-helpers b/portmason/pm-helpers
index 5b91095..910acdd 100755
--- a/portmason/pm-helpers
+++ b/portmason/pm-helpers
@@ -72,16 +72,30 @@ can_mkdir_p() {
 }
 
 cleanup_tail() {
-  if declare -F live_tail_stop >/dev/null 2>&1; then
-    live_tail_stop || true
-  else
-    local pid="${__LIVE_TAIL_PID:-${PM_TAIL_PID:-${TAIL_PID:-}}}"
-    if [[ -n "$pid" ]] && kill -0 "$pid" 2>/dev/null; then
-      kill -- -"${pid}" 2>/dev/null || true
-      kill "${pid}" 2>/dev/null || true
+    local owner="${__LIVE_TAIL_OWNER_BASHPID:-}"
+
+    # An executed child pm-* script inherits the PIDs but does not own them.
+    if [[ -n "$owner" && "$owner" != "${BASHPID}" ]]; then
+        return 0
     fi
-  fi
-  unset __LIVE_TAIL_PID __LIVE_TAIL_GUARD_PID PM_TAIL_PID TAIL_PID
+
+    if declare -F live_tail_stop >/dev/null 2>&1; then
+        live_tail_stop || true
+    else
+        local pid="${__LIVE_TAIL_PID:-${PM_TAIL_PID:-${TAIL_PID:-}}}"
+
+        if [[ -n "$pid" ]] && kill -0 "$pid" 2>/dev/null; then
+            kill -- "-${pid}" 2>/dev/null || true
+            kill "${pid}" 2>/dev/null || true
+        fi
+    fi
+
+    unset \
+        __LIVE_TAIL_OWNER_BASHPID \
+        __LIVE_TAIL_PID \
+        __LIVE_TAIL_GUARD_PID \
+        PM_TAIL_PID \
+        TAIL_PID
 }
 
 trap cleanup_tail EXIT INT TERM
@@ -628,81 +642,89 @@ have() {
 info() { log INFO "$@"; }
 
 live_tail_autostart() {
-  : "${LIVE_TAIL:=1}"
-  [[ "${LIVE_TAIL}" == "1" ]] || return 0
-  [[ -n "${LOG_FILE:-}" ]] || return 0
+    : "${LIVE_TAIL:=1}"
+    [[ "${LIVE_TAIL}" == "1" ]] || return 0
+    [[ -n "${LOG_FILE:-}" ]] || return 0
+    [[ "${__HAS_TTY_BEFORE_REDIRECT:-0}" == "1" ]] || return 0
+    [[ -w /dev/tty ]] || return 0
+
+    if [[ -n "${__LIVE_TAIL_GUARD_PID:-}" ]] &&
+       kill -0 "${__LIVE_TAIL_GUARD_PID}" 2>/dev/null; then
+        return 0
+    fi
 
-  [[ "${__HAS_TTY_BEFORE_REDIRECT:-0}" == "1" ]] || return 0
-  [[ -w /dev/tty ]] || return 0
+    unset __LIVE_TAIL_PID __LIVE_TAIL_GUARD_PID
 
-  # Already running?
-  if [[ -n "${__LIVE_TAIL_GUARD_PID:-}" ]] && kill -0 "${__LIVE_TAIL_GUARD_PID}" 2>/dev/null; then
-    return 0
-  fi
-  unset __LIVE_TAIL_PID __LIVE_TAIL_GUARD_PID
+    export __LIVE_TAIL_OWNER_BASHPID="${BASHPID}"
 
-  local parent_pid="$$"
+    local parent_pid="$$"
 
-  (
-    set +e
+    (
+        set +e
 
-    # Start tail
-    tail -n 200 -F "$LOG_FILE" > /dev/tty 2>&1 &
-    tail_pid=$!
+        tail -n 200 -F "$LOG_FILE" > /dev/tty 2>&1 &
+        tail_pid=$!
 
-    # Publish PIDs back into environment via a temp file (cannot export back to parent directly)
-    pidfile="/tmp/pm-live-tail.${parent_pid}.pid"
-    printf '%s\n' "$tail_pid" > "$pidfile" 2>/dev/null || true
+        pidfile="/tmp/pm-live-tail.${parent_pid}.pid"
+        printf '%s\n' "$tail_pid" > "$pidfile" 2>/dev/null || true
 
-    # Watch parent; when it dies, stop tail
-    while kill -0 "$parent_pid" 2>/dev/null; do
-      sleep 0.25
-    done
+        while kill -0 "$parent_pid" 2>/dev/null; do
+            sleep 0.25
+        done
 
-    kill -- "-$tail_pid" 2>/dev/null || true
-    kill "$tail_pid" 2>/dev/null || true
-    wait "$tail_pid" 2>/dev/null || true
+        kill -- "-$tail_pid" 2>/dev/null || true
+        kill "$tail_pid" 2>/dev/null || true
+        wait "$tail_pid" 2>/dev/null || true
 
-    rm -f "$pidfile" 2>/dev/null || true
-    exit 0
-  ) &
+        rm -f "$pidfile" 2>/dev/null || true
+        exit 0
+    ) &
 
-  guard_pid=$!
-  export __LIVE_TAIL_GUARD_PID="$guard_pid"
+    guard_pid=$!
+    export __LIVE_TAIL_GUARD_PID="$guard_pid"
 
-  # Best-effort: read tail pid
-  pidfile="/tmp/pm-live-tail.${parent_pid}.pid"
-  for _ in 1 2 3 4 5; do
-    if [[ -s "$pidfile" ]]; then
-      __LIVE_TAIL_PID="$(cat "$pidfile" 2>/dev/null || true)"
-      export __LIVE_TAIL_PID
-      break
-    fi
-    sleep 0.05
-  done
+    pidfile="/tmp/pm-live-tail.${parent_pid}.pid"
 
-  return 0
+    for _ in 1 2 3 4 5; do
+        if [[ -s "$pidfile" ]]; then
+            __LIVE_TAIL_PID="$(cat "$pidfile" 2>/dev/null || true)"
+            export __LIVE_TAIL_PID
+            break
+        fi
+
+        sleep 0.05
+    done
+
+    return 0
 }
 
 live_tail_stop() {
-  # Stop guard (which stops tail), plus best-effort direct kill.
-  local guard="${__LIVE_TAIL_GUARD_PID:-}"
-  local tailp="${__LIVE_TAIL_PID:-}"
+    local owner="${__LIVE_TAIL_OWNER_BASHPID:-}"
+
+    if [[ -n "$owner" && "$owner" != "${BASHPID}" ]]; then
+        return 0
+    fi
 
-  unset __LIVE_TAIL_GUARD_PID __LIVE_TAIL_PID
+    local guard="${__LIVE_TAIL_GUARD_PID:-}"
+    local tailp="${__LIVE_TAIL_PID:-}"
 
-  if [[ -n "$guard" ]] && kill -0 "$guard" 2>/dev/null; then
-    kill "$guard" 2>/dev/null || true
-    wait "$guard" 2>/dev/null || true
-  fi
+    unset \
+        __LIVE_TAIL_OWNER_BASHPID \
+        __LIVE_TAIL_GUARD_PID \
+        __LIVE_TAIL_PID
 
-  if [[ -n "$tailp" ]] && kill -0 "$tailp" 2>/dev/null; then
-    kill -- "-$tailp" 2>/dev/null || true
-    kill "$tailp" 2>/dev/null || true
-    wait "$tailp" 2>/dev/null || true
-  fi
+    if [[ -n "$guard" ]] && kill -0 "$guard" 2>/dev/null; then
+        kill "$guard" 2>/dev/null || true
+        wait "$guard" 2>/dev/null || true
+    fi
 
-  return 0
+    if [[ -n "$tailp" ]] && kill -0 "$tailp" 2>/dev/null; then
+        kill -- "-$tailp" 2>/dev/null || true
+        kill "$tailp" 2>/dev/null || true
+        wait "$tailp" 2>/dev/null || true
+    fi
+
+    return 0
 }
 
 load_adapter_helper() {
diff --git a/portmason/pm-recover-static-partial-from-page b/portmason/pm-recover-static-partial-from-page
index 0f1202a..5efa7c0 100755
--- a/portmason/pm-recover-static-partial-from-page
+++ b/portmason/pm-recover-static-partial-from-page
@@ -1,10 +1,15 @@
 #!/usr/bin/env bash
+set -euo pipefail
+
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
+: "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
 # Purpose: Recover a missing static partial from one managed HTML region.
 # Usage:   pm-recover-static-partial-from-page PAGE MARKER [OUTPUT_FILE]
 # Example: pm-recover-static-partial-from-page index.html CATALOG-BRANDS-FULL-LIST
 
-set -euo pipefail
-
 usage() {
   cat >&2 <<'USAGE'
 Usage:
diff --git a/portmason/pm-setup b/portmason/pm-setup
index 5629159..f87890e 100755
--- a/portmason/pm-setup
+++ b/portmason/pm-setup
@@ -1,12 +1,10 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-export DEPLOY_ENV="${DEPLOY_ENV:-${DEPLOY_ENV:-}}"
-
-#. "/usr/local/bin/sys-cd-project-root"
-
 : "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}" 
 export CUR_SCRIPT
+: "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
 
 is_deploy_snapshot_dir() {
     [[ "$(basename -- "$(dirname -- "$PWD")")" == "deploy" ]]
@@ -15,13 +13,9 @@ is_deploy_snapshot_dir() {
 main() {
     export PROJECT_SERVICES=
 
-    : "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
-
     local deploy_sync_active=0
     local deploy_sync_force=0
     local sync_owner_host=""
-    local arg
-    local -a setup_args=()
     local -a sync_args=(--prepare)
 
     case "${PM_DEPLOY_SYNC_FORCE:-}" in
@@ -30,18 +24,7 @@ main() {
             ;;
     esac
 
-    for arg in "$@"; do
-        case "$arg" in
-            --refresh-from-root|--force-deploy-sync)
-                deploy_sync_force=1
-                ;;
-            *)
-                setup_args+=("$arg")
-                ;;
-        esac
-    done
-
-    . "${PORTMASON_SHARE}/pm-helpers" "${setup_args[@]}"
+    [[ ${REFRESH_FROM_ROOT:-0} == 1 || ${FORCE_DEPLOY_SYNC:-0} == 1 ]] && deploy_sync_force=1
 
     if is_deploy_snapshot_dir; then
         sync_owner_host="$(hostname 2>/dev/null || printf 'unknown')"
@@ -57,27 +40,38 @@ main() {
         die "--refresh-from-root is only valid from deploy/<env>"
     fi
 
-    "${PORTMASON_SHARE}/pm-generate-config"
+    #"${PORTMASON_SHARE}/pm-generate-config"
+    log INFO "pm-setup: generating project configuration"
+log INFO "pm-setup: ENV_FILE=${ENV_FILE:-<unset>}"
+log INFO "pm-setup: SITE_DIR=${SITE_DIR:-<unset>}"
+log INFO "pm-setup: generator=${PORTMASON_SHARE}/pm-generate-config"
+
+if "${PORTMASON_SHARE}/pm-generate-config"; then
+    log INFO "pm-setup: project configuration generated"
+else
+    rc=$?
+    fatal_error "pm-generate-config failed with exit code ${rc}" "$rc"
+fi
 
     stage_portmason "archive" "cagent" "*cosign*" "*log" # Exclude archive/, cagent, cosign files, log files
 
     # run the adapter provisioning script, if it exists
     local file="${PORTMASON_SHARE}/pm-provision-${ADAPTER_CODE}"
-    [[ -r "$file" ]] && . "$file" "${setup_args[@]}"
+    [[ -r "$file" ]] && . "$file" "$@"
 
     # run the optional adapter setup script
     file="${PORTMASON_SHARE}/pm-setup-${ADAPTER_CODE}"
-    [[ -r "$file" ]] && . "$file" "${setup_args[@]}"
+    [[ -r "$file" ]] && . "$file" "$@"
 
     # run the optional runtime setup script
     file="${PORTMASON_SHARE}/pm-setup-${RUNTIME_CODE}"
-    [[ -r "$file" ]] && . "$file" "${setup_args[@]}"
+    [[ -r "$file" ]] && . "$file" "$@"
 
     # if a custom setup file exists, it must handle all deployment tasks for the project,
     # Otherwise, run the standard runtime deploy followed by the standard adapter deploy
     file="${SCRIPT_DIR}/setup"
     if [[ -r "$file" ]]; then
-        . "$file" "${setup_args[@]}"
+        . "$file" "$@"
         local rc=$?
 
         case "$rc" in
@@ -97,15 +91,15 @@ main() {
     else
         # run the optional runtime deploy script
         file="${PORTMASON_SHARE}/pm-deploy-${RUNTIME_CODE}"
-        [[ -r "$file" ]] && . "$file" "${setup_args[@]}"
+        [[ -r "$file" ]] && . "$file" "$@"
 
         # run the optional adapter deploy script
         if declare -F pm_bridge_deploy >/dev/null 2>&1; then
             log INFO "pm-setup: using bridge deploy hook: pm_bridge_deploy"
-            pm_bridge_deploy "${setup_args[@]}"
+            pm_bridge_deploy "$@"
         else
             file="${PORTMASON_SHARE}/pm-deploy-${ADAPTER_CODE}"
-            [[ -r "$file" ]] && . "$file" "${setup_args[@]}"
+            [[ -r "$file" ]] && . "$file" "$@"
         fi
     fi
 
@@ -116,4 +110,8 @@ main() {
     fi
 }
 
+pm_setup_arg REFRESH_FROM_ROOT "--refresh-from-root" export
+pm_setup_arg FORCE_DEPLOY_SYNC "--force-deploy-sync" export
+pm_commit_args "$@"
+
 main "$@"
diff --git a/portmason/pm-sync-cm-from-root b/portmason/pm-sync-cm-from-root
index b79c553..45b7c0f 100755
--- a/portmason/pm-sync-cm-from-root
+++ b/portmason/pm-sync-cm-from-root
@@ -1,7 +1,11 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
 : "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
 
 STATE_VERSION="1"
 STATE_FILE_NAME=".pm-deploy-sync.state"
diff --git a/portmason/pm-sync-deploy-from-root b/portmason/pm-sync-deploy-from-root
index b79c553..45b7c0f 100755
--- a/portmason/pm-sync-deploy-from-root
+++ b/portmason/pm-sync-deploy-from-root
@@ -1,7 +1,11 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
 : "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
 
 STATE_VERSION="1"
 STATE_FILE_NAME=".pm-deploy-sync.state"
diff --git a/portmason/pm-update-docker-env-file b/portmason/pm-update-docker-env-file
index 5804d4e..3ce54ce 100755
--- a/portmason/pm-update-docker-env-file
+++ b/portmason/pm-update-docker-env-file
@@ -1,6 +1,12 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
+: "${CUR_SCRIPT:=$(basename "${BASH_SOURCE[0]:-${0:-}}")}"
+export CUR_SCRIPT
+
+: "${PORTMASON_SHARE:="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-${0:-}}")" && pwd -P)"}"
+. "${PORTMASON_SHARE}/pm-helpers" "$@"
+
 # Usage:
 #   pm-update-docker-env-file [path-to-compose]
 #
